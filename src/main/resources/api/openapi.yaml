---
openapi: 3.0.0
info:
  description: >-
    Sample OpenAPI specification file for generating some operations to 
    demonstrate how to use OpenAPI in a ginger services project.
  version: 1.0.0
  x-ebay-todo:
    autogenerated: >-
      The title below is autogenerated. Please review that it accurately 
      reflects your project. After confirmation or update, please remove 
      this todo block from your contract.
  title: lrpdemo
servers:
  - url: 'http://localhost:8080/samplesvc/v1'
    description: 'Development server running on localhost'
paths:
  /sample/version:
    get:
      summary: Returns the version response.
      description: >-
        Returns the version response.
      tags:
        - Sample
      operationId: version
      x-ebay-codegen-resource:
        meta:
          pageName: version
      security:
        - app_scope_auth:
            - 'https://api.ebay.com/oauth/scope/@public'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: string
  /sample/hello:
    get:
      summary: Returns a simple hello response.
      description: >-
        Returns a simple hello response.
      tags:
        - Sample
      operationId: hello
      x-ebay-codegen-resource:
        meta:
          pageName: hello
      security:
        - app_scope_auth:
            - 'https://api.ebay.com/oauth/scope/@public'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: string
  /sample/helloclient:
    get:
      summary: Returns a simple hello response from ginger client.
      description: >-
        Returns a simple hello response from ginger client.
      tags:
        - Sample
      operationId: testClient
      x-ebay-codegen-resource:
        meta:
          pageName: testClient
      security:
        - app_scope_auth:
            - 'https://api.ebay.com/oauth/scope/@public'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: string
components:
  securitySchemes:
    app_scope_auth:
      type: oauth2
      description: >-
        The security definitions for this API. Please check individual
        operations for applicable scopes.
      flows:
        clientCredentials:
          tokenUrl: 'https://api.ebay.com/identity/v1/oauth2/token'
          scopes:
            'https://api.ebay.com/oauth/scope/@public': This scope would allow public access to an operation.